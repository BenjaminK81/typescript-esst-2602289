export function Demo() {
    let DARK_MODE = false; // Typ gleich als wert definiert
    const isDarkMode = DARK_MODE; // In weiterer Anwendung ist dieser z.B. erstmal nur FALSE
    let errorCode;
    errorCode = "ERROR_500";
}
