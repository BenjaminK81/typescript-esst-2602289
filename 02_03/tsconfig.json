{
  "compilerOptions": {
    "target": "ES3", // kompiliert in altes JS -> es kommt zur Fehlermeldung
    "module": "CommonJS", //
    "skipLibCheck": true // verhindert Fehlermeldung, indem im node_modules nicht nach ES3 gesucht wird
  },
  "include": ["**/*.ts"], // kompiliert alle Dateien Ordnerübergreifend
  "exclude": ["michnicht.ts"] // bis auf exclude-Dateien
}
